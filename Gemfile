# frozen_string_literal: true

source "https://rubygems.org"

# gem "rails"

#AWS SDK (Software Development Kit) for Ruby. Contains libraries.
gem 'aws-sdk-s3'

#debuggint tool for making breakpoints in ruby
gem 'pry'

# for parsing xml, when working with api's parsing is needed
gem 'nokogiri'

